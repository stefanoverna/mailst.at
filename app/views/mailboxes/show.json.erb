<%=
  {
    mailbox: {
      credentials_valid: @mailbox.credentials_valid?,
      credentials_verified: @mailbox.credentials_verified?,
      folders_form_partial: (@mailbox.credentials_valid? ? render("folders_form.html.slim", mailbox: @mailbox) : "")
    }
  }.to_json.html_safe
%>
