en:
  simple_form:
    "yes": 'Yes'
    "no": 'No'
    required:
      text: 'required'
      mark: '*'
      # You can uncomment the line below if you need to overwrite the whole required html.
      # When using html, text and mark won't be used.
      # html: '<abbr title="required">*</abbr>'
    error_notification:
      default_message: "Please review the problems below:"
    labels:
      mailbox:
        label: "Name"
        seconds_between_reports: "Send every"
        report_time_hour: "Send at"
        folders:
          imap_name: "IMAP Folder"
          max_seconds_to_process: "Max. defer time"
          label: "Rename into"
